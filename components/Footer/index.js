import React from "react";
import styles from "./styles.module.css";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <h4>Build with Next.js</h4>
    </footer>
  );
};

export default Footer;
